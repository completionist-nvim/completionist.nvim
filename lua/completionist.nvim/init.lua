--[[ this module exposes the interface of lua functions:
define here the lua functions that activate the plugin ]]

local main = require("completionist.nvim.main")
local config = require("completionist.nvim.config")
